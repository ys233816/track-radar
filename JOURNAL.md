# 项目日志：赛道雷达是怎么长出来的

> 记录 2026-09-19 ~ 09-20 的完整决策过程。**这份日志的价值不在结论，在弯路**——哪些想法被否了、为什么否、哪一步差点走错。

---

## 一、起点：三个被否掉的方向

### ❌ 方向 1：AI 客服

最初的想法是"做个 AI 产品代替电商客服，按公司规定回答"。

**被否的理由**（AIPM-Anchor 诊断）：

- "代替客服"是**一个工具品类，不是一个业务场景**——没有具体用户任务、没有触发时刻、没有失败成本口径
- **AI 挂错了节点**："根据规定回答问题"里，最不需要 AI 的恰恰是"检索规定"——那是搜索的活
- **完全没有边界设计**，而这是致命伤：AI 客服幻觉 = 越权承诺 = 真金白银的赔付

诊断里那句话后来证明是对的：*面试官第一句一定是"AI 答应赔钱但不合规定怎么办"——答不上来，项目就从"AI 产品"变成"事故现场"。*

### ❌ 方向 2：上 GitHub 找一个项目

用户当时的反应是"换一个方向，你上 GitHub 找一个适合的"。

**被否的理由**：GitHub 上按 star 排序捞出来的全是**基础设施和平台**（docling、WAN、RAGFlow 这类），不是业务产品。而且更根本的问题是——**别人仓库里的项目不是你的项目**，说到底是拿别人的代码去面试。

> 不过这个弯路产出了一个有用的认知：**GitHub 不是找"项目"的地方，是找"任务原语"的地方。** 找一个别人已经实现好的难做的功能（OCR、diff、检索），然后在它上面做业务锚点——这个用法是对的。

### ✅ 方向 3：赛道雷达

用户自己提出来的：*"做一个网页，用户可以选一个 AI 细分赛道，让 AI 联网调研 5 个产品，再对话生成带图表和对比表的完整 HTML 报告页。"*

**这个想法明显更好**——输入明确、产出明确、可视化效果好。所以诊断的重点不是"要不要做"，而是"哪里会做砸"。

---

## 二、Anchor：功能锚点怎么定的

诊断出的**结构性问题**：

> 想要"AI 生成完整报告页"。但**写报告是模板的活，不是 AI 的活**。AI 该干的是它真正擅长的三件事：跨源归一、可信度判断、缺口识别。

**正确的架构由此确定**：

```
AI 只产结构化 JSON（值 + 来源 URL + 置信度）→ 代码渲染 HTML + 图表
```

这一条改掉了原始方案：不是"让 AI 吐 HTML"，而是"AI 吐数据，程序画图"。区别在于——**幻觉变得可见、可核查、可修订**。

**同时定下的两条底线**：

1. 每个数字必须带来源，查不到就标「未获取」，**绝不允许编**
2. 不做"一键生成完整报告"——越自动化，越容易编

---

## 三、Chain：主链路和那个关键的两阶段设计

主链路定为「可信对比表链路」，L1-L8：

```
触发 → 输入 → 预处理 → 核心处理 → 输出 → 反馈 → 状态沉淀 → 下一次/主动触发
```

### 两阶段是被现实逼出来的

原本想用 Claude 的 `output_config.format`（结构化输出）配合 `web_search` 一次搞定。**实测失败**：模型在有 web_search 时会写 Markdown 而不是受约束的 JSON，SDK 直接抛 `Failed to parse structured output`。同样的参数有时成功有时失败。

**拆成两阶段后反而更好**：

| 阶段 | 干什么 | 为什么这样分 |
|---|---|---|
| 1 · 调研 | 联网搜集事实，产出原始记录 + **实际访问过的 URL 清单** | 不约束输出，让它自由发挥 |
| 2 · 归一 | 把记录整理成字段，`source_url` 必须从阶段 1 的清单里**原样选取** | 不联网，只做整理 |

**阶段 1 产出的 URL 清单，正好成为阶段 2 做来源校验的基准。** 这是整个反幻觉机制的地基。

### 另一个实测发现：长请求必须流式

非流式的调研请求在 **180 秒**被网关掐断（错误 `terminated`）。改流式后 72 秒跑完。这是硬约束，不是优化。

---

## 四、三条不变量（写在代码里，不写进 prompt）

产品的全部价值就是这三条。**prompt 拦不住它们，所以写进代码。**

| # | 不变量 | 触发它被发现的真实事件 |
|---|---|---|
| 1 | **编造率为 0** — 声称的 URL 必须与本次检索真实返回的集合比对 | 一直存在，属于设计 |
| 2 | **推导字段不得比依据字段更可信** — 起步月费与起步价原文必须同源 | 见下方"抓到的最深的 bug" |
| 3 | **「未获取」是正常输出** — 数值含多数字时拒绝猜测 | Jasper 的价格是"$69 月付或 $59 年付"，无法确定取哪个 |

`npm test` 有 14 条断言盯着这三条，测试数据全部取自真实调研输出。

### 抓到的最深的 bug

跑完一轮后核对表格与图表，发现：

> **Fathom 的「起步价原文」显示未获取，但图表画出了 $19。**

不一致的根源不在渲染，在归一：模型给「起步月费」选了**另一条独立证据**，而那条证据和「起步价原文」说的不是一回事。

继续追，发现 Writesonic 更严重：

```
起步价原文来源：writesonic.com 官方博客  → $99/月 或年付 $79
起步月费来源  ：geekflare.com 第三方评测 → 39
```

**两个数都支持不了 39。** 而且模型还因为 geekflare 是第三方，把它标成了 `verified`（已核实）——**置信度判定的方向完全反了**。

修复方式是加代码层不变量：推导字段必须与依据字段**同源**，不同源直接降级。

---

## 五、评测：跑出来的真数字，和我自己犯的三个错

语料 = **10 轮真实联网调研 / 370 个字段槽位**，全部归档在 `eval/corpus/`。

| 指标 | 实测 |
|---|---|
| 编造率 | **1.62%**（6/370），3 轮触发拦截 |
| 跨字段同源违反 | **3 → 0** |
| 价格覆盖率 | **78.9% → 55.6%**（修复的代价） |
| L4-A 内容命中 | **93.3%**（14/15，无偏等距抽样） |
| 真广告落地页占比 | **17.11%**（45/263）—— **未修，最大缺口** |

### 错误 ①：度量 bug 让数字虚高一倍

第一版算出"广告页占比 **36%**"。数字可疑，查下去发现正则把**搜索工具产生的 `#1#1` 分片锚点**也当成了广告落地页。拆开后真实值是 **17.11%**。

> 教训：**任何看起来吓人的指标，先怀疑度量本身。**

### 错误 ②：差点用偏置样本冒充证据

原本打算用「高频常规」桶（定义就是"有值且通过校验"）来测内容命中——在里面测出 10/10 是**结构性的**，不构成任何证据。改成从全部 326 个有值槽位做**无偏等距抽样**，才拿到可信的 14/15。

> 这段**特意留在评测报告里没删**，因为它比那个漂亮数字有价值得多。

### 错误 ③：生成的报告每次重跑都产生虚假 diff

报告头带墙上时钟 → 每次重跑 `git status` 都脏 → **"评测到底变了没有"永远答不了**。改成**语料指纹**（sha256），语料不变则产物逐字节一致。

### 最有价值的一条失败样本

> Fathom 的「部署方式」填进了帮助文档的引导句：*"Use this section as a quick way to audit that you've installed our Desktop App…"*
> **它通过了来源校验** —— URL 真实存在、来自官方帮助中心。

**这证明了 URL 校验拦不住"答非所问"。** 是这个项目里最硬的一段面试材料：不是"我做了个 AI 工具"，而是"我知道我的防护在哪一层失效，并且把它量化了"。

---

## 六、发布

- **线上演示**：https://ys233816.github.io/track-radar/ （GitHub Pages，从 `main` /docs 发布）
- **单文件版**：`npm run build:demo` → `track-radar-demo.html`，81 KB，双击即开，无后端/密钥/CDN
- **仓库**：https://github.com/ys233816/track-radar

演示版里**自进化钩子是能真跑的**：改数据写进浏览器 localStorage，同一字段改满 2 次会当场升级为「高频错误点」。不是截图。

---

## 六·补、补上最大的缺口：来源分级

前面所有防线的共同盲区：**URL 校验只能判断「这个链接真的存在」，判断不了「它够不够格支撑这条事实」。**

实测中 33.13% 的引用属于**能点开但没资格**——17.1% 是广告落地页（带 `utm_`/`gclid` 等投放参数），16.7% 是聚合目录站（vendr.com、techreviewer.co、geekflare 之类）。

### 设计上最关键的一点

**分级是 `source_url` 的纯函数**，所以能**回溯应用**到已有的 10 轮历史语料——不用为了拿到等级重跑一遍调研（那要 40 分钟）。

而且它是**代码规则，不是让模型自己判断**，与项目其余部分一致：能写成不变量的事，不写进 prompt。

### 分 6 级

| 等级 | 含义 | 占比 |
|---|---|---|
| T1 | 官方定价页 | 4.9% |
| T2 | 官方文档 / 帮助中心 | 18.3% |
| T3 | 官方其他（首页·博客·法务） | 31.2% |
| T4 | 第三方评测 | 11.8% |
| T5 | 聚合目录站 | 16.7% |
| T6 | 广告落地页 | 17.1% |

判定规则：
- **T5 / T6 对任何字段都不够格**（信息是转述或营销投放）
- **价格类字段不接受 T3**（官网首页/博客上的价格常是引流话术、档位不全或已过时）

### 过程中修的两个 bug

**① 子域名漏判（影响面很大）**

第一版只在**路径**里找 `docs/help/pricing`，结果 `help.otter.ai`、`docs.granola.ai`、`docs.writesonic.com` 这类把信号放在**子域名**里的全部漏判成「官方其他」。T2 因此只有 3.8%，而 T3 虚高到 45.6%。改成匹配**域名 + 路径**后：T2 → 18.3%，T3 → 31.2%。

**② 非法 URL 被降级成 T4**

`gradeSource("不是URL")` 原本返回 T4（第三方），等于**让一个垃圾串看起来像个合格引用**。改为返回 NONE。

> 还有一个 bug 是**测试自己写错的**：我给所有 URL 都传了 `Otter.ai` 作为产品上下文，却拿 `granola.ai`、`writesonic.com` 的 URL 去判——官方域名判定靠的是"域名里是否含产品名"，上下文配错当然认不出来。5 个"失败"里有 5 个是测试的错，不是代码的错。

### 界面上怎么呈现

表格里每个来源多一个徽章（`定价`/`文档`/`官方`/`三方`/`聚合`/`广告`）。不够格的标红。

最典型的例子：**`grammarly.com` 的引用标红为「广告」**——域名看着是官方，但 URL 带投放参数，它是广告落地页。以前这条看起来完全正常，现在一眼能看出来。

---

## 六·再补、自纠错闭环：不够格就重搜，还不行就诚实降级

分级只解决「发现问题」。发现之后呢？加了两段：

```
来源不够格 → 定向重搜一次 → 够格了？ → 采用
                    ↓ 仍不够格
              降级为「未获取」（宁缺勿猜）
```

重搜时的系统提示写了：**严禁把原来源再交一遍**。

### 实测结果（三轮，52 个触发字段）

| 轮次 | 触发 | 救回 | 降级 | 成功率 |
|---|---:|---:|---:|---:|
| r08 | 20 | 8 | 12 | 40% |
| r09 | 12 | 7 | 5 | 58% |
| r10 | 20 | 14 | 6 | 70% |
| **合计** | **52** | **29** | **23** | **55.8%** |

但同一轮内**产品间方差极大**：

| 产品 | 触发 | 救回 | 成功率 |
|---|---|---|---|
| Jasper（r10） | 7 | 6 | 86% |
| Grammarly（r09） | 6 | 6 | 100% |
| Grammarly（r10） | 8 | ? | — |
| Copy.ai（r10） | 4 | 0 | **0%** |
| Copy.ai（r08） | 5 | 1 | 20% |

**用平均值描述它是不诚实的**——它取决于**这个产品的广告投放有多激进**。Copy.ai 和 Grammarly 在搜索里投放很重，够格的来源基本被广告页挤掉了。

### 最有价值的发现：模型没说谎，是搜索结果本身就烂

系统提示明确写了「不许拿原来源充数」。模型照做了吗？**没有——它换成了另一个同样是广告的页面。**

```
补证前：jasper.ai/                            （首页，带 utm_source）
补证后：jasper.ai/platform?gclid=CjwKCAjw...  （产品页，Google Ads 落地页）
```

**这不是模型不听话，是它搜到的东西就这些。** 广告落地页和聚合目录站在搜索结果里排名本来就很靠前，你让它"去找个更好的来源"，它能找到的候选池里大部分还是这些。

> 这条比"补证成功率多少"重要得多：**它说明「换个来源」这个思路本身有上限。**
> 想要真正解决，得从搜索策略层面下手（限定站点、直接用厂商文档站），而不是指望模型在一次 `web_search` 里自己挑出好东西。

### 修的计数 bug

第一版日志报「Jasper 补证救回 6/6」，同一行下面又写「降级 3 个」——**自相矛盾**。

原因：`_rescued` 统计的是「**拿到了新值**」，不是「**新来源够格**」。统计发生在重新分级**之前**，所以把"换了个新值但新来源同样不够格"也算成了救回。

改成在**降级之后**统计存活数。这类 bug 很典型：**指标算在了错误的时点上，报出来的数字就是自我安慰。**

顺带把「救回」的格子打上 `source_retried` 标记，让语料**自我描述**——否则评测只能靠「降级理由里有没有某句话」去猜，那是脆弱的字符串匹配。

### 覆盖率代价

v1 → v2：**不够格引用 33.72% → 0%**，但**字段值覆盖率 95.6% → 70.0%**。

**归零不是能力提升，是口径变化**——不够格的引用被**删掉了**，不是被**修好了**。覆盖率的同时下降就是代价的收据。只报前者不报后者，读者会以为来源质量变好了。

---

## 七、还没做的（下次打开从这里继续）

| 缺口 | 说明 |
|---|---|
| **B 层端到端评分** | 8 项 rubric 已设计，未逐条评分 |
| **L8 提示准确性** | 需要注入构造的校正历史来测真/假阳性 |
| **v1 样本量** | 修复后只有 2 轮 / 90 槽位，"方向可信、幅度不可信" |
| **赛道覆盖** | 只有 2 条赛道（AI 会议纪要 / AI 写作），不能外推 |
| **来源分级的假阳性** | 官方定价页带联盟参数（`?fpr=`）仍判 T1 —— 这类参数不在投放跟踪清单里，尚未处理 |

> ✅ **来源分级已完成**（2026-09-20），见下一节。

### 两条自己给自己的规矩

1. **公开材料里的数字不能靠记忆。** 我曾在 README 里写"4 轮实跑"（真实是 7 轮），又猜"240 槽位"（真实是 280）。现在每个数字发布前都要从数据重算一遍。
2. **指标可能被坏正则悄悄污染。** 见上面错误 ①。
