# 简历条目

> 这份文件同时是**作品集材料**和**证据边界说明**。每条 bullet 下面都标了它对应仓库里的哪个文件、处于哪个完成度——**能被追问倒的句子不该出现在简历上，能被追问倒的边界应该主动说出来**。

---

## 可粘贴版本

**赛道雷达 · AI 赛道可比信息抽取器**

**项目背景**：做 AI 赛道速览时，真正的难点不是搜不到产品，而是①不同来源口径不一无法直接比较，②无法判断哪些说法可信。独立设计并实现了联网调研工具，产出的对比表**每个字段都带来源链接与置信度**，查不到的明确标注「未获取」而非推测填充。

- **把 AI 从「生成报告」移到「产结构化字段」**：模型只输出 `{值, 来源URL, 置信度}` 三元组，排版与图表由代码渲染。这个拆分让"每个数字可追溯到源头"从愿望变成可校验的机制——若直接让模型产出报告正文，任何错误都会被排版成看起来权威的图表，且无从追溯。

- **设计并实现代码层的来源校验**：逐字段比对模型声称的 URL 与本次检索**真实返回过的 URL 集合**，对不上即判定为编造、清空该值并降级为「未获取」。7 轮真实联网调研共 280 个字段槽位，拦下 5 例编造来源（编造率 1.79%）。过程中发现这类错误**连片出现**——同一产品的多个字段会一起被拦，据此把审计单位从「字段」改为「产品」。

- **用真实调研输出构建离线评测并驱动迭代**：语料为 7 轮真实调研的 280 个字段槽位，只对**可机械判定**的指标用代码计算，需主观判断的维度一律留空并标注「需人工」。据此发现「起步月费」与「起步价原文」可能引用不同来源、数值互相矛盾（实测第三方 $39 vs 官方年付 $79），加入「推导字段必须与依据字段同源」的代码层不变量后，**同源违反从 3 降到 0**；代价是**价格覆盖率从 78.9% 降到 55.6%**——这是主动取舍：宁可不给数字，也不给两个来源互相打架的数字。

- **量化并定位了防护失效的边界**：对 263 个有值字段做无偏等距抽样（15 条）标注，内容命中率 93.3%，并定位到唯一失败样本——某产品的「部署方式」填入了帮助文档的引导句（*"Use this section as a quick way to audit that you've installed…"*）。**该样本通过了全部来源校验**，据此确认「URL 可验证 ≠ 内容正确」，把内容质量列为需人工判定的独立维度，而非试图用规则覆盖。

---

## 事实与边界说明

每条 bullet 对应仓库中的证据文件，以及它**能做到哪一步**。

| # | Bullet | 证据来源 | 完成边界 |
|---|---|---|---|
| 1 | AI 只产结构化字段 | [`lib/research.mjs`](lib/research.mjs) 两阶段流水线；[`lib/schema.mjs`](lib/schema.mjs) 的 `coerceProduct` | **实现完成** |
| 2 | 代码层来源校验 | [`eval/report.md`](eval/report.md)：编造率 1.79%（5/280） | **离线验证完成** |
| 3 | 评测驱动迭代 | [`eval/report.md`](eval/report.md) 修复前后对比；[`test/invariants.mjs`](test/invariants.mjs) 14 条断言 | **离线验证完成** |
| 4 | 防护失效边界 | [`eval/manual-labels.json`](eval/manual-labels.json) + [`eval/report.md`](eval/report.md) | **离线验证完成（人工标注）** |

### ⚠️ 每条 bullet 的限制（面试要主动说，别等被问）

**第 2 条**：只能说明"这套校验拦住了 5 例"，**不能说明"消除了幻觉"**。只测了 280 个槽位、2 条赛道。而且分母是"系统产出的槽位"，不是"全网该有的字段"——它是**拦截率**，不是幻觉率。

**第 3 条**：修复后的样本只有 **2 轮 / 90 槽位**，**方向可信、幅度不可信**。价格覆盖率下降是**有意的取舍**，不是退步——但必须同时报出来，只报"同源违反降到 0"就是选择性陈述。

**第 4 条**：**标注者是 AI 助手本身，不是领域专家，且未做标注者间一致性检验**。15 条样本量小，一个 case 就值 6.7 个百分点。

### ❌ 以下均无证据，不能写

真实用户 / 上线 / 满意度 / 留存 / 转化 / 准确率提升 / 降本增效 / 服务企业数量。

项目**没有任何线上验证**——演示页已发布，但无埋点、无对照、无真实用户。

### 【待补充】

- **来源质量维度全貌**：已测出**真广告落地页占比 17.11%**（45/263，引用带 `utm_`/`gclid` 等投放参数的页面），但**来源分级尚未实现**——这是当前最大的已知缺口
- **B 层端到端评分**：8 项 rubric 已设计，尚未逐条评分
- **赛道覆盖**：仅 2 条赛道（AI 会议纪要 / AI 写作），**不能外推**

---

## 收紧口径的备选版本

如果面的是偏保守的岗位，或你不想在简历上谈未发布的量化结果，可以只保留前两条，把第 3、4 条合并为：

> 构建离线评测脚手架（7 轮真实调研 / 280 字段槽位），只计算可机械判定的指标；识别并量化了一类防护失效边界——**存在通过全部来源校验、但内容答非所问的字段**，据此把内容质量列为独立的人工判定维度。

---

## 相关文件

- [项目日志](JOURNAL.md) —— 这个项目是怎么长出来的，包括三个被否掉的方向和过程中犯的错
- [评测报告](eval/report.md) —— 全部真实数字，`npm run eval` 可一键复现
- [在线演示](https://ys233816.github.io/track-radar/)
